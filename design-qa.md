# 집지기 프로덕션 디자인 QA

최신 개인 데모의 화면 검증과 최초 디자인 비교 이력을 함께 기록합니다. 아래 최신 검증은 로컬 프로덕션 빌드 기준이며 공개 배포 완료를 뜻하지 않습니다. 이전 계약 조회와 신뢰 경계 수정은 [별도 감사 기록](docs/audits/2026-08-31-trust-and-consistency.md)에 보존했습니다.

## 2026-08-31 개인 데모 검증

- 환경: Next.js standalone 서버, 임시 PGlite, Chromium. 운영 DB와 실제 발송 채널은 사용하지 않았습니다.
- 모바일: 393 × 850. 두 홈 구성을 체험 설정에서 각각 선택해 다시 시작하고 첫 카드 순서를 확인했습니다.
- 데스크톱: 1440 × 1000. 계약에서 갱신 안내를 접수한 뒤 같은 브라우저에서 그로스 역할로 전환해 메시지 요청 1건을 확인했습니다.
- 모바일 설정 제목이 고정 헤더에 가리던 문제를 수정했습니다. 제목이 헤더 아래에 놓이는지 실제 화면과 E2E 위치 검사로 확인했습니다.
- 모바일 계약과 메시지 탐색, 가로 넘침, 로그아웃 후 API 차단을 E2E에 포함했습니다. 네 여정 모두 통과했고 수동 확인에서 브라우저 콘솔 오류는 없었습니다.

| 화면 | 실제 캡처 | 확인한 내용 |
| --- | --- | --- |
| 계약 만료 먼저 보기 | [모바일 홈](.design-qa/portfolio-readiness/owner-risk-mobile.png) | D-28 계약과 갱신 안내를 첫 카드에 표시 |
| 오늘 일정 먼저 보기 | [모바일 홈](.design-qa/portfolio-readiness/owner-agenda-mobile.png) | 같은 시나리오의 운영 일정을 첫 카드에 표시 |
| 개인 데모 관리 | [모바일 설정](.design-qa/portfolio-readiness/demo-settings-mobile.png) | 제목 가림 없음, 홈 구성 선택, 초기화 동의와 복구 불가 안내 |
| 메시지 센터 | [데스크톱 메시지](.design-qa/portfolio-readiness/messages-desktop.png) | 샌드박스 접수 결과와 건물 및 호실 이름 표시 |
| 그로스 관제 | [데스크톱 관제](.design-qa/portfolio-readiness/growth-desktop.png) | 자신의 메시지 1건과 데모 데이터 범위 안내 |

홈 이미지의 날짜와 지표는 캡처 당시 합성 시나리오입니다. 관제 수치를 실제 고객 실험의 성과로 해석하지 않습니다. 화면 이미지를 코드가 생성한 UI로 직접 캡처했으며 디자인 목업으로 대체하지 않았습니다. 이 검증만으로 전체 제품의 모든 기기, 색상 대비와 보조 기술에 결함이 없다고 보증하지 않습니다.

## 최초 디자인 비교 기준

- 선택 시안: `prototype/.design-qa/source-home-normalized.png`
- 프로덕션 캡처: `.design-qa/production-mobile-passed.png`
- 병렬 비교본: `.design-qa/comparison-mobile-passed.png`
- 뷰포트: 두 화면 모두 393 × 850
- 상태: 성수 리버하임, 위험 우선 실험안, 갱신·미납·수리 조치 전 초기 상태

## 최초 비교 당시 판정

당시 비교한 화면 범위에서 차단 수준의 P0, P1, P2 시각 결함은 발견하지 못했습니다. 이후 변경 전체에 대한 보증은 아닙니다.

초기 비교에서 모바일 헤더가 흰색으로 분리되어 야간 히어로의 연속성이 깨지는 P1 차이와 Next.js 개발 표시가 하단 내비게이션을 가리는 P1 차이를 확인했습니다. 헤더를 시안과 같은 야간 배경으로 통합하고 개발 표시를 비활성화한 뒤 같은 뷰포트에서 재검증했습니다. 불투명 로고 배경과 헤더 색상 차이도 동일 색상 토큰으로 맞췄습니다.

## 집지기 브랜드 변경 후 검증

- 당시 임대인 화면: `.design-qa/mobile-home-final.jpg`
- 당시 그로스 관제 화면: `.design-qa/operator-growth-final.jpg`
- 집 모양과 확인 표시를 결합한 벡터 워드마크가 랜딩, 로그인, 모바일 헤더와 사이드바에서 같은 형태로 렌더링되는지 확인했습니다.
- 기존 병렬 비교 이미지는 정보 구조와 레이아웃 결정의 이력을 보여주는 자료로 보존했습니다.

## 의도적으로 유지한 제품 차이

사용자가 선택한 3번 시안의 야간 히어로, 곡선 전환, 보라색 CTA, 코랄 위험 신호, 흰색 카드 구조를 유지했습니다. 동시에 1번과 2번 시안에서 선호한 수납률, 입주율, 수리 진행 지표와 개인화 인사를 첫 화면에 합쳤습니다. 이 차이는 시각 오류가 아니라 승인된 기능 결합입니다.

## 확인 항목

- 모바일 안전 영역과 고정 하단 내비게이션이 콘텐츠를 가리지 않음
- 헤더, 히어로, 지표 카드, 우선 조치 카드의 간격과 모서리 반경이 일관됨
- 갱신 CTA와 건물 선택 컨트롤이 실제 동작하며 키보드 포커스 스타일을 제공함
- 텍스트와 상태 색상만으로 의미를 전달하지 않고 아이콘과 문구를 함께 제공함

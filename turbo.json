{
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [],
      "env": ["AUTH_SECRET", "DATABASE_URL", "DB_DIR", "MESSAGE_WEBHOOK_SECRET", "NEXT_PUBLIC_APP_URL", "UPSTASH_REDIS_REST_URL", "UPSTASH_REDIS_REST_TOKEN", "KV_REST_API_URL", "KV_REST_API_TOKEN", "VERCEL_GIT_COMMIT_SHA", "VERCEL_ENV"]
    },
    "@jipjigi/web#build": {
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["AUTH_SECRET", "DATABASE_URL", "DB_DIR", "MESSAGE_WEBHOOK_SECRET", "NEXT_PUBLIC_APP_URL", "ALLOW_DEMO_AUTH", "UPSTASH_REDIS_REST_URL", "UPSTASH_REDIS_REST_TOKEN", "KV_REST_API_URL", "KV_REST_API_TOKEN", "VERCEL_GIT_COMMIT_SHA", "VERCEL_ENV"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "build:packages": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}
